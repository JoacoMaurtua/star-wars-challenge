import React from 'react';

const SectionHeader = ({ title }) => {
    return <div style={{ height: '56px', fontWeight:'bold', marginTop: '16px', marginBottom: '9px', fontSize: '19px'}}>{title}</div>;
};

export default SectionHeader;
